# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# Next.js
.next/
out/
# ⚠ ANCHORED. An unanchored `build/` matches a directory called `build` AT ANY DEPTH, and on
# 2026-08-17 it silently excluded `app/ideas/build/` — a real route — from the repository.
# The rule was only ever meant to catch a build output directory at the project root.
/build/

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Clerk
.clerk/

# Misc
.DS_Store
Thumbs.db
*.log
.vercel/

tsconfig.tsbuildinfo
