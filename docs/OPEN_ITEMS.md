# SCRUTINISE — OPEN ITEMS

*The single register of things that are known, unfinished, and not written down anywhere a person
will look. Created 2026-08-24 (C3 Lane F4). C1 reported this file missing; its absence contributed
to a production incident, because an item that lives only in a sprint report is an item nobody
re-reads.*

**The rule for this file:** an item leaves only when it is **done and verified**, or when somebody
**decides not to do it** — and a decision is recorded here with its reason, not silently deleted.
An item that is merely stale stays, with its date showing.

**One line per item. Every item names: what a user sees · what it would take · who decides.**

---

## ⛔ BLOCKED — needs a person, not more work

| id | item | what a user sees today | who decides |
|---|---|---|---|
| **OI-1** | **`ots-reports` — delete 421 of 497, or the brief's 68?** Measured against the gov.uk content API, 497 of 497 readable: 76 published by the OTS, 421 by somebody else. The brief authorised removing "news and speeches" (~69); the publisher test says 84.7% of the collection does not belong. 148 of the 421 are held in no other collection. | A question about tax simplification can return *Renew your driving licence* or *Apply online for a UK passport*, filed as an Office of Tax Simplification report. | **Charlie** — D-1 in `INGEST_C3_REPORT.md`. Script staged: `c2/ots-filter.ts --apply`. |
| **OI-2** | ⚠⚠ **PARTLY DONE 2026-08-27 02:20 UTC, AND THE HALF THAT IS MISSING IS THE HALF USERS SEE.** Charlie ran the purge: Neon is at **18,103,959** compiled (was 18,272,452), `et-decisions` 293,403 → 161,753, the seven whole collections at zero, `ftsVector` dropped. **Layer three — `l2-purge-index.ts` — did NOT run.** Measured 27 Aug 15:00: `corpus_fts` still holds **18,272,377 rows including 36,919 from the seven purged collections**. ⚠ It **cannot be run from this machine**: OOM inside LanceDB's Rust layer at batch 4,000 and again at 400, counting 131,650 ids against an 18M-row table with no scalar index on `id` — CLAUDE.md §17. Steps 6, 7 and 8 of `C3_EXECUTE.sh` are also still unrun, as is all of `C3A_EXECUTE.sh` (`ots-reports` still reads 497). | 36,919 rows whose source row was deleted twelve hours ago are still being returned, plus the 131,650 `et-decisions` landing pages. | **Charlie** — decision B-1 in `INGEST_CENSUS_C1_B_REPORT.md`. Recommended: try a scalar index on `corpus_fts.id` first (45 s for the equivalent on `corpus_chunks`), then the Heavy Job Runner if that is not enough. |
| **OI-26** | ⚠ **THE PURGE RETIRED AND BLOCKED `et-decisions` ENTIRELY, THOUGH IT ONLY DELETED THE LANDING PAGES.** `l2-purge.ts` runs `UPDATE corpus_targets SET retired=true, blocked=true` for the collection named by each target. For seven of the eight that is correct. The eighth target was `et-decisions-landing`, and the collection still holds **161,753 real employment tribunal judgments** (census: MEASURED, 131,161 of 133,641 decisions = 98.1%). | **Nothing, for retrieval** — `runSearch()` does not read `corpus_targets`; checked, not assumed. But 161,753 sections read as *retired* in every report that filters on that flag, and are added to the retired total. | **Charlie** — decision B-2. One `UPDATE` reverses it; `l2-purge.ts` also needs a guard so a target narrower than its collection cannot retire the collection. |
| **OI-3** | **`uk-treaties` (3,250) and `tax-treaties-dta` (324) can be returned by NO query at any setting.** Verified live and two-sided on 24 Aug: 20/20 scoped to themselves, **0/20** through the debates scope and 0/20 through committees, **3/20 and 4/20 with the tier filter alone**. They are in the `parliamentary` tier, named in `NON_DEBATE_PARLIAMENTARY`, and display-typed TREATY, which no stream admits. | A treaty question is answered from `uk-treaties-fcdo` (23,372 sections, reachable because it happens to be typed DEBATE) and never from its two siblings — on a type distinction no user made. | **Charlie.** ⚠ The stated blocker is now GONE: `corpus-map.ts` says this "cannot be measured today — the validated set has ZERO debates questions", and **Gold v2 shipped 11 debates questions (Q1–Q11) on 22 August**. The comment is stale; the decision is measurable. |
| **OI-4** | **House of Lords judicial archive — ~760 judgments we hold none of.** The Lords was the final court of appeal until 30 Jul 2009 and Find Case Law does not publish it at all. OPL v3.0, commercial use permitted, under $2. **Gate 1 is RED:** every `parliament.uk` host returns **403 with a Cloudflare challenge**, with and without a browser UA, including the site root. | Ten pre-2001 authorities were run through the real `runSearch()`: 10 of 10 absent, 3 of 10 returned a *different case with a similar name*, 0 of 10 returned nothing. The absence never presents as an absence. | **Charlie** — a route that is not Node `fetch` from this machine. Do not build around the challenge page. |

## 🔧 BUILT, NOT DELIVERED — code is in, users have not seen it

| id | item | why it has not reached a user | next step |
|---|---|---|---|
| **OI-5** | **B5 — legislation titles, 54.2% → 99.1%.** `ukpga/Geo4/5/83` now resolves to *Vagrancy Act 1824*, and 1,575 citations repoint to the id form that actually holds sections. | `loadActTitles` is read at **index build** time and the title is baked into the `corpus_fts` body. The resolver is fixed; the index still holds the old rows. | `fts-refresh.ts --corpus=primary-acts-pre-2000 --from=db`, then redeploy `fts-serve`. Step 8 of `C3_EXECUTE.sh`. ⚠ Moves BM25 document frequencies — take no baseline across it. |
| **OI-6** | **B2/B4 — whole-body dot leaders excluded from retrieval.** 249,256 rows whose entire text is `Article 31 . . . .` have been *labelled* in every search result since Surface 1 and returned anyway. | The gateway filter is committed and takes effect on the next request — **but only for rows `section_repeals` holds.** B2's 249,256 is a **floor**: a further ~1,487 exist that the table never held. | Deploy, then run the B3 backfill (step 7) to add the missing ones. |
| **OI-7** | **B3 — ~32,040 partially repealed sections [95% CI 25,956–40,088] carry no label at all.** Live law with removed subsections. `section_repeals` has never held a row of this kind. | The backfill is written, dry-run and blocked (OI-2). | Step 7 of `C3_EXECUTE.sh`. No index rebuild, no redeploy — the join is live. |

## 📏 MEASURED, NOT ACTED ON

| id | item | the measurement | note |
|---|---|---|---|
| **OI-8** | **The 503 `et-decisions` orphans are mostly not fetchable — now measured over the WHOLE population, not a sample.** | **26 Aug: all 503 read. 51 has-pdf (10.1%) · 452 no-attachment · 0 gone · 0 error** — the 200-row sample's projection of 53 was within two. The boundary separates cleanly: **332 of the 452 carry the six-digit pre-2013 Scottish case numbering and 93 more carry a 41xx Scottish office number — 425 of 452 (94%)** — against **0 of the 51** with a judgment. | Boundary declared in `CORPUS_SCOPE.md` in B1's words: *"Scottish employment tribunal decisions before 2013 are not published and are not held."* The **51 are staged for re-fetch through the general `govuk-content` path** — `c3a/et-orphans-refetch.ts`, step 5 of `docs/C3A_EXECUTE.sh`. |
| **OI-9** | **`historic-hansard`'s earliest `itemDate` is `1013-06-24`.** | Straight off the live table while generating `CORPUS_SCOPE.md`. | A year-parsing artefact, almost certainly `1913`. **Addendum D-4, 2026-08-26: record it and move on** — one row's parse artefact, and no user-facing surface facets on that date today. ⚠ It becomes urgent the moment a date filter ships over 4.6M sections. **The cause is owned by `BRIEF_INGEST_REPEALED.md` §5, not by this stream** — do not duplicate the work here. ⚠ That brief is not in the repository as of 26 Aug (OI-23). |
| **OI-10** | **`si-pre-2010` has 15,784 instruments with no title on either id form**, and the publisher enumeration has no title for them either. | B5 measured 83.3% → 84.4% across all six legislation collections; the regnal fix is worth +1,651 and the residue is 23,646. | The regnal repair does not touch these. A different cause, unexamined. |
| **OI-11** | **`vec-hygiene.ts`'s header quotes a stale cost.** It records ~22.5 s per delete predicate on `corpus_chunks`, measured 11 Aug. | Measured 24 Aug: `sectionId IN (2000)` returns in **1.5 s** — a BTree index (`sectionId_idx`) landed since. `corpus_vec` is 10.1 s and `corpus_fts` 6.9 s (neither is indexed on its key). | A comment nobody re-measured would have justified an unnecessary redesign. |
| **OI-16** | **`<Citation URI>` markup covers 2–5% of the cross-references that are actually in the text**, so any inbound-citation count taken from it alone is a FLOOR reported as a total. Measured over 6,045 documents: 5.4% of body mentions of the Human Rights Act carry markup, 1.8% of the Equality Act, **0% of CRAG 2010**. This is a property of legislation.gov.uk's data, not of our code. | Measured over 6,045 documents by `probe` against the bulk CLML: 5.4% Human Rights Act, 1.8% Equality Act, 0% CRAG 2010. | 25-H's `citation_edge` adds a second `text` detector and keeps the two apart in a `detection` column; `legislation_edges`' `cites` rows have no such column and are markup-only. **Anything quoting `cites` as "the citations" is quoting ~2%.** |
| **OI-18** | **93,772 act-name spans in `citation_edge`'s text pass resolved to nothing, and 11.3% of the rows that DID resolve are not in a provision at all.** The unresolved are short forms ("the Taxes Act 1988"), pre-1963 Acts under the other id form, and Acts we do not hold — counted, never dropped silently. The non-provision rows are an Act named in an SI's TITLE, long title or explanatory note: real references, but not provisions that break. | 93,772 of 1,429,037 spans unresolved (6.6%); 73,238 of 649,202 text rows have `source_provision_ref IS NULL` (11.3%). CRAG's 182 inbound becomes 149 with them filtered; the Equality Act's 1,868 becomes 1,552. ▶ **RE-MEASURED 26 Aug (GRAPH 4A §3): 97,095 spans** over the same 132,990 documents — a **3.5% discrepancy with 25-H's own counter that is not explained**, flagged rather than reconciled. **29.9%** of them sit in a document that also cites one of twelve research-target Acts (leave-one-out band 23.0–28.0%; the decision survives every one). | ⚠⚠ **THE LEVER NAMED HERE IS THE WRONG ONE, and that is the sprint's finding.** Classifying the 60 commonest unresolved names (30,472 spans) by cause: **title-absent 59.2% · title-mismatch 31.6% · short-form 9.3%.** The commonest unresolved name in the corpus is *"the Interpretation Act (Northern Ireland) 1954"* — 3,732 spans, a FULL title, not an abbreviation of anything. **Short-form resolution would recover under a tenth of the gap.** The dominant cause is titles absent from `corpus_acts` — `apni` above all (see OI's `v37` flag: fifty years of NI primary legislation, 2,602 references). **Ingest the absent Acts and re-measure: decision Q4 in `GRAPH_4A_REPORT.md`.** For a repeal work-list today, filter `source_provision_ref IS NOT NULL` — the query surface does not do it for you, deliberately, because a title reference is still a reference. |

| **OI-22** | **27 seven-digit ENGLISH employment tribunal rows have no judgment attached, and the Scottish pre-2013 boundary does not explain them.** | Of the 452 `et-decisions` orphans with no attachment, 120 are seven-digit; 93 of those carry a 41xx Scottish office number, leaving **27** in English offices (13, 19, 24, 26, 27, 66), 114 of the 120 dated **2013 or later** — 66 of them from 2014 alone. | Small, and it is the residue after a boundary that otherwise separates 0-of-51 against 425-of-452. Either gov.uk lost the attachments or those decisions were listed and never published. Nobody has read one. |
| **OI-24** | **`ots-reports` holds LANDING PAGES, not reports — and so will the re-seeded collection.** | Every one of the 497 rows is `format = null`, median **399 words**. Three genuine OTS bodies read by hand are gov.uk summary pages (*"The OTS report on Claims & Elections will be published in autumn 2020"*). Of the 222 documents in the publisher's own universe, **143 (64.4%) carry at least one PDF attachment — 300 PDFs in total — and the seeder fetches none of them.** | The same shape as `building-regs` (21 rows, all landing pages, no PDF fetch ever written). Fixing it is a fetch change in `processGovUk`, which already exists one function along in `processGovukContent` — that path DOES fetch attachments. Quote the collection as "222 OTS documents, landing pages held" until then. |
| **OI-27** | **46 of 91 collections in the census are UNMEASURED, and the two largest are among them.** `historic-hansard` (4.64M sections) and `scottish-parliament-or` (1.04M) have no walked denominator; nor do the legislation collections whose walk is still running. Separately, six collections that printed complete yesterday have real, measured gaps: `bills-api` **10.4%** · `petitions` **36.6%** · `echr-hudoc` **55.0%** · `committees-reports` **58.1%** · `hmrc-manuals` **80.9%** · `members-interests` **84.1%**. | UNMEASURED prints the held count and the word — no percentage, no tick. So a user reading the email learns the size of what we hold and, honestly, nothing about the fraction. | **CC** — decision B-3: Find Case Law next (Atom feed works, 74,896 units, largest MEASURED gain available), then historic-hansard (~2,400 month-page fetches). Holyrood and Senedd are JavaScript search pages with no index and should STAY UNMEASURED with their declared scope rather than be forced into a number. |
| **OI-25** | ⚠⚠ **SUPERSEDED IN PART, 27 Aug: the census replaces this for 20 collections and confirms it for the rest.** `corpus_census` now holds a publisher-walked denominator for 20 collections and the daily email reads that table, never `est_sections`. The remaining 46 read UNMEASURED. The original finding below stands for every collection not yet walked. **31 OF 77 LIVE COLLECTIONS ANSWER "HOW COMPLETE ARE WE?" WITH THEIR OWN ROW COUNT.** | `corpus_targets.est_sections` equals the held row count EXACTLY for 31 live collections — **23 of them flagged `est_is_confirmed = true`** — so `held / est_sections` is 100% by construction, whatever is missing. Five more sit within 0.1% (a stale copy of the same number). | The same closed loop GRAPH 3B found behind the storage alert line, and the same family as a check that cannot fail. `ots-reports` was one of the 31 and now has a real external denominator (**222**, from gov.uk's own organisation field) staged in `c3a/ots-measured.ts`. **The other 30 need one each, and that is research per collection, not a sweep.** Artefact: `docs/census/C3A_denominator_sweep.json`. |
| **OI-23** | **`BRIEF_INGEST_REPEALED.md` is not in the repository.** | The addendum's ownership split gives that brief the dot-leader detector, the repeal records, the user-facing wording, the *as enacted* fetch and the partial repeals — and says **"where they touch, REPEALED wins"**. §9 also requires B1's case-law boundary wording and REPEALED §2's repeal wording to **match** and to land together or in consecutive sprints. | Nothing in this stream can be aligned against a document that is not on disk. **B1 was not started for this reason among others.** ⚠ Do not write the wording twice. |

## 🧨 TRAPS — recorded so they are not rediscovered

| id | item |
|---|---|
| **OI-12** | **A quoted identifier in a LanceDB predicate matches NOTHING and raises NOTHING.** `"id" = 'x'` → 0; `id = 'x'` → 1. Measured on all three tables. It is also ~70× *faster*, because it prunes every fragment — so it looks like a working optimisation. A `delete()` carrying the quoted form removes 0 rows and reports success. `fts-hygiene.ts` and `vec-hygiene.ts` both use the bare form; `l2-purge-index.ts` broke with them and its count-before guard is what caught it. |
| **OI-13** | **`isRepealedPlaceholder` has now been defeated three times by the same shape.** V36: the bare number (`31 . . .`). C2 Lane 2: the provision label (`Article 31 . . .`). C3: **the provision number's own multi-letter suffix** (`12ZA . . .`, `234ZA`, `502GC`, `164FG`) — one letter was always fine, which is why it survived two fixes. Every fix has been "strip one more leading thing". The next costume is worth predicting rather than waiting for. |
| **OI-14** | ⚠⚠ **CORRECTED 2026-08-26 — THE MECHANISM WAS WRONG AND THE REAL DEFECT IS WORSE.** C3 recorded that `source-audit.ts` asserts `minSize: 5000` on the OTS collection page, that the page 404s, and that **the check has therefore passed since V1**. It has not. The URL returns HTTP 404, `!r.ok` short-circuits every later check, and the rule has printed **⛔** since the file was written (unchanged since 2026-06-04) — the `minSize` line is never reached. A simulation of the audit's own check precedence was compared against a live run and agreed on **44 of 44** rules. ▶ **What is true:** **14 of 50 rules print ⛔ and nothing acts on the output**, and that one column merges **6 genuinely dead URLs** (OTS, PACE codes, NI Law Commission, LDA Hansard debates, ONS datasets, ECHR query) with **5 bot challenges** (Erskine May, Bills pages, Ofwat, SSRN, HoC Library) and 1 transient — three different problems needing three different answers, shown identically. Also measured: **1 rule that CANNOT PASS** (`jsOnly` tests `bodySnippet.length > 200` against a 200-character slice, so FCA Handbook reports ⛔ at HTTP 200 whatever the server sends), **1 false alarm on a healthy source** (TNA Legislation ⚠️ for an XML tag tested against a 200-char slice), **1 that passes on an empty result set**, **10 that cannot fail** (a floor 20–120× below the delivered size, or no assertion at all), and **5 that assert the gov.uk SEARCH answers rather than that the source publishes what the collection claims** — the `oecd` rule is green off `q=OECD` while that collection holds 505 gov.uk pages and no OECD content. ⚠ **BAILII is reported ✅ HTTP 200 `text/html` and is serving a bot check** ("Making sure you're not a bot!"). The PRINCIPLE — a size threshold is not an existence check — stands and is worth keeping; this instance is not an example of it. Artefact: `docs/census/C3A_source_audit_rules.json`. **Nothing in `source-audit.ts` has been changed:** the addendum requires the list first. |
| **OI-19** | ⚠⚠ **THE TWO GRAPH TABLES DO NOT AGREE ON WHAT A PRE-1963 ACT IS CALLED, AND A JOIN ON `gid` SILENTLY DROPS EVERY ONE OF THEM.** `legislation_edges` keeps the URI's own calendar form (`ukpga/1961/33`); `citation_edge` normalises to the regnal form legislation.gov.uk treats as canonical (`ukpga/Eliz2/9-10/33`). Neither is wrong. Measured 26 Aug: `legislation_edges` `cites` has **0** regnal-form targets, `citation_edge` has **717** (3,531 rows); looked up under the `citation_edge` form, three sample Acts return **0 rows** where the URI form returns 59, 50 and 50. It also explains **1,980 of the 2,094** pairs that appear to exist only in `legislation_edges` — same edge, different identity string. ⚠ **The loss presents as a coverage result, not as a bug** — the same shape as OI-15. The alias map already exists in TWO copies (`buildAliasMap` in `extract-citation-edges.ts`, `identitiesFor` in `v37-citation-gaps.ts`); a third copy is how OI-15 happened. **Prerequisite for retiring anything or adding any layer — decision Q3 in `GRAPH_4A_REPORT.md`.** |
| **OI-20** | ⚠⚠ **WE HOLD THE SCHEDULED AGREEMENT FOR 39 OF 288 DOUBLE TAXATION ORDERS (13.5%).** A double taxation agreement takes domestic effect by Order in Council under TIOPA 2010 s.2 and **the treaty text is scheduled to the Order** — so the corpus should already hold it. All 288 Orders hold text and 238 carry a `made-under` edge to TIOPA, but **249 hold no schedule section at all**: the three operative articles are there and the agreement is not. By era: 13 of 32 from 2018 on, **26 of 256 before 2018**. ⚠ **The absence presents as a short document, not as an error** — the 2% markup failure one level down. A tax question about a treaty is answered from the Order's operative articles and reads as an answer. The Orders are on legislation.gov.uk and the fetch is free. **Decision Q5 in `GRAPH_4A_REPORT.md`.** |
| **OI-21** | **MLI positions are not held and cannot come from legislation.gov.uk.** The OECD Multilateral Instrument modifies many double taxation agreements at once **without amending each Order**, so an agreement read off legislation.gov.uk can be out of date without saying so. Twenty `corpus_acts` titles match "multilateral" and none is the MLI (EU banking directives, a fisheries convention, the Multilateral Investment Guarantee Agency Act 1988, a 1951 social-security Order); every section-title match is a debate or committee report *mentioning* it. The per-country reservations and notifications are published by the OECD. **Declared in the `inbound()` coverage block as `treaty-obligations` NOT BUILT; acquisition is decision Q6.** |
| **OI-15** | **`extract-cites-edges.ts` never opened 37% of the Acts in the bulk file, and nobody noticed for seven weeks.** Its zip-entry filter is `-(\d{4})-`, which requires a CALENDAR year, so every regnal-year filename (`ukpga-Geo3-41-52-revised-data.xml`) fails to match: **2,431 of 132,990 documents skipped — 1,650 ukpga, all 660 `aep`, all 58 `apgb`**, all pre-1963. Proved by consequence, not by reading the regex: of 121,279 `cites` edges, **exactly 0** have a regnal-year source, while 29,800 edges of other types do. July's fix for regnal ids widened the URI *parser* (`GRAPH_TIER1_REPORT.md` §3.1) and never reached the entry *filter* — a separate code path. ⚠ The general shape: **a fix applied to one of two places that must agree, with no check that they agree.** ▶ **CODE FIXED 26 Aug 2026 (GRAPH 4A §2)** — `extract-cites-edges.ts` now imports the same `ENTRY_RX`, so the two places cannot drift; the second copy in `extract-madeunder-edges.ts` was measured HARMLESS (0 SI-type entries carry a regnal filename) and shares the constant anyway. ⚠ **`legislation_edges` is still NOT re-extracted, deliberately.** The hole is **924 edges = 0.76%**, zero on all four control Acts, and `citation_edge` already holds 98.1% of `cites`' pairs plus 226,516 more — so the recommendation is to RETIRE the `cites` rows rather than repair them. **Decision Q2 in `GRAPH_4A_REPORT.md`.** |

---

## Closed

| id | item | why it left |
|---|---|---|
| **OI-17** | **"Neon is past its alert line" — WITHDRAWN, it was never an item.** Raised by 25-H on the strength of a 17.5 GB "alert line" quoted in `setup-edges-table.ts`. | **Closed 2026-08-26 as a false alarm, by Charlie.** There is no Neon storage ceiling: `neon.max_cluster_size` is 16 TiB and storage is a BILL, not a WALL — $0.35/GB-month against a $15/month budget, so 19 GB is ~$6.65 = 44%, quiet. ⚠ The project had ALREADY killed this figure: GRAPH 3B §4.1 traced its provenance to a closed loop (our constant cited the handoff; the handoff cited our constant), GRAPH 3C §5 retired it for the cost line, and `serve-observer.ts` prints "There is NO storage ceiling to hit" in the LIVE code. 25-H resurrected it by trusting a July header comment without asking whether it was still true. **The comment is now corrected in place, in `setup-edges-table.ts`, because leaving it is how it came back a second time.** |
