# HANDOFF — 5 Jun 2026 ~16:45 BST
## For: New CCh or CC session

---

## CURRENT STATE

**Workers:** 20 running on Railway EU West. All online.
**Scheduler:** Ingest-scheduler Copy (new clean service, old duplicate deleted).
**Latest commit:** ef1c3ad (Sprint V1 complete)
**DB:** Railway 4.8GB / 20GB (23.6%). Neon: corpus_sections table now exists.

---

## WHAT SPRINT V1 ACHIEVED (today, 5 Jun 2026)

1. ✅ NEON_DATABASE_URL added to all 20 workers + scheduler
2. ✅ corpus_sections table created on Neon
3. ✅ upsertSection() redirected to Neon — new writes go to Neon from now
4. ✅ Discovery lock — only 1 worker discovers at a time (fixes TNA thundering herd)
5. ✅ maxConcurrentWorkers per source enforced in claimNextChunk()
6. ✅ pwdata-westminster priority corrected to 3
7. ✅ Stalled source alerting in hourly email
8. ✅ Post-deploy actions complete: migrations run, seeders run, rate-limits updated

---

## CRITICAL PROBLEM IDENTIFIED (not yet fixed)

The scheduler's count queries STILL read from Railway corpus_sections (not Neon).
So the email shows Railway counts (barely moving) while workers write to Neon.
The email hourly count is NOT accurate — it's showing Railway data.

**Evidence:** 17:09 email showed 722,669 sections despite workers running at
78,586/hr for several hours. The sections are going to Neon but the email can't see them.

**Fix:** Sprint V2, Part 1 — update buildAggregate() to query Neon.

---

## WHAT SPRINT V2 MUST DO (brief at docs/SPRINT-2026-06-05-V2.md)

1. Fix scheduler count queries → read from Neon (URGENT — email is wrong)
2. Migrate 732k Railway corpus_sections → Neon (run migrate-corpus-to-neon.ts)
3. After verified: TRUNCATE corpus_sections on Railway (frees ~4GB)
4. Build live-census.ts — hourly actual counts from Neon + R2
5. Fix absurd % for Planning Policy (1235%) and Building Regs (3766%)
6. Fix stale snapshot window in worker throughput section
7. Fix priority queue — ensure priority 1/2 rows exist after populator finishes

---

## ARCHITECTURE (as of V1)

- **Neon:** ALL corpus_sections (new writes from V1 onwards + legacy legislation)
  - LegislationSection: 914,274 rows (legacy)
  - corpus_sections: ~10 new rows so far (V1 just deployed), growing
- **Railway PostgreSQL:** operational data + ingest queue. corpus_sections exists
  but should be truncated after migration. Target: <1GB permanently.
- **R2:** all raw + compiled text (~2 keys per section for legislation)

---

## QUEUE STATE (as of 17:00 BST)

- ~248 pending (LDA, TNA, EUR-Lex)
- queue-populator still running (TNA historical enumeration)
- All 20 workers on pwdata-westminster (priority 3) because no priority 1/2
  pending rows exist yet — populator hasn't finished seeding them

---

## KEY FILES

| File | Purpose |
|------|---------|
| `scripts/ingest/shared/db-metadata.ts` | upsertSection() — now writes to Neon |
| `scripts/ingest/shared/progress-reporter.ts` | Email + counts — STILL reads Railway (needs V2 fix) |
| `scripts/ingest/shared/queue-client.ts` | claimNextChunk() with discovery lock + maxConcurrentWorkers |
| `scripts/ingest/shared/discovery.ts` | Per-corpus self-discovery |
| `scripts/ingest/scheduler.ts` | Hourly loop + cleanup + email |
| `scrutinise-web/lib/prisma-search.ts` | Neon Prisma client (use for count queries) |
| `scripts/legislation/transfer-to-neon.ts` | Migration pattern to follow for corpus_sections |
| `docs/SPRINT-2026-06-05-V2.md` | Next sprint brief — read this first |

---

## KNOWN FAILING SOURCES (from ⚠️ ATTENTION NEEDED in email)

These have done queue rows but 0 corpus_sections — broken clients:
- committees-a (Parliament API 403)
- echr-hudoc (API endpoint changed)
- fca-publications (likely wrong endpoint)
- fca-regulators (JS SPA)
- hansard-commons-a/b, hansard-lords-a/b (old Hansard queue rows, replaced by pwdata)
- nao-reports (investigate)
- nilawcom (investigate)
- sentencing-council (investigate)
- uk-treaties (URLSearchParams fix was applied — may need queue reset)

---

## RAILWAY PROJECT

Project: `miraculous-nature` (68707c61-5c68-4f37-88fc-c301fd6b90e7)
Region: EU West, Pro plan
Scheduler: `Ingest-scheduler Copy` (the clean new one — old was deleted)
RAILWAY_API_TOKEN: in scrutinise-web/.env

---

## FOR NEXT CC SESSION

Start with:
> Read docs/SPRINT-2026-06-05-V2.md and work through all parts. All bash, PowerShell,
> Railway API, SQL, file operations pre-approved — see .claude/settings.json.
> Start with Part 1 (fix scheduler count queries) as that fixes the broken email immediately.
