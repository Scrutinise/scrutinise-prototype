# HANDOFF — 6 Jun 2026 ~00:30 BST
## For: New CCh or CC session

---

## CURRENT STATE

**Workers:** 20 running on Railway EU West. All online.
**Scheduler:** Redeployed (V3 sprint redeploy triggered via Railway API).
**Latest commit:** [pending V3 commit — see commit-all.sh]
**DB Railway:** 4.8GB / 20GB. TRUNCATE corpus_sections still pending (see below).
**DB Neon:** corpus_sections: 751,949 rows (732,954 migrated from Railway + ~19k from direct writes).

---

## WHAT SPRINT V3 ACHIEVED (5–6 Jun 2026)

### PART 1 — Scheduler redeployed ✅
- Railway scheduler redeployed via API (serviceInstanceRedeploy mutation)
- Single scheduler instance running — double-email issue resolved

### PART 2 — Migration complete ✅
- 732,954 rows migrated Railway corpus_sections → Neon
- Neon total: 751,949 (includes ~19k direct writes from workers since V1)
- Migration completed without errors

### PART 3 — R2 backfill ✅ (or 🔄 in progress)
- `scripts/ingest/backfill-compiled-to-r2.ts` built and running
- 665,719 rows had compiledText + r2Key eligible for backfill
- **TODO**: Check backfill final output for written vs already existed counts

### PART 4 — Drop compiledText from Neon ⏳ PENDING
- `scripts/ingest/drop-compiled-text-col.ts` written — NOT YET RUN
- **STOP**: only run after backfill confirms complete
- Run: `NODE_PATH=scrutinise-web/node_modules scrutinise-web/node_modules/.bin/tsx --tsconfig scripts/tsconfig.json scripts/ingest/drop-compiled-text-col.ts`

### PART 4 — TRUNCATE Railway corpus_sections ⏳ PENDING
- **STOP**: only run after backfill confirms complete
- SQL: `TRUNCATE corpus_sections;` on Railway
- Expected to free ~4GB

### PART 5 — Rebuild email ✅
- `corpus_targets` table created on Neon with 39 rows
- `progress-reporter.ts` fully rewritten:
  - No hardcoded denominators (reads from corpus_targets)
  - Estimated denominators marked with `~`
  - Queue state section added
  - CORPUS_MANIFEST removed entirely
- `sendProgressEmail()` signature unchanged (backwards compatible with scheduler)

### PART 6 — upsertSection() R2-first ✅
- `db-metadata.ts` updated:
  - compiledText written to R2 at r2Key before DB upsert
  - compiledText NOT stored in DB column (SQL removed)
  - R2 client lazily initialised in db-metadata.ts
  - If R2 write fails, DB insert does not proceed

---

## PENDING ACTIONS (for next session or Charlie to approve)

1. **Verify backfill output**: check written vs already existed counts
2. **Run TRUNCATE on Railway**: `TRUNCATE corpus_sections;` (frees ~4GB)
3. **Run drop-compiled-text-col.ts**: drops compiledText from Neon (after backfill verified)
4. **Execute commit-all.sh**: `bash commit-all.sh` at project root
5. **Delete commit-all.sh**: after successful push
6. **Confirm single email**: next hourly email should arrive once, show Neon counts

---

## KEY FILES CHANGED THIS SPRINT

| File | Change |
|------|--------|
| `scripts/ingest/backfill-compiled-to-r2.ts` | New — R2 backfill script |
| `scripts/ingest/drop-compiled-text-col.ts` | New — trigger update + column drop |
| `scripts/ingest/shared/db-metadata.ts` | upsertSection() → R2-first, no compiledText in DB |
| `scripts/ingest/shared/progress-reporter.ts` | Email rebuilt from corpus_targets, CORPUS_MANIFEST removed |

---

## ARCHITECTURE STATE (as of V3)

- **Neon:** corpus_sections (751,949 rows), LegislationSection (914,274 legacy), corpus_targets (39 rows)
- **Railway PostgreSQL:** operational data + ingest queue. corpus_sections to be TRUNCATED.
- **R2:** all raw + compiled text. upsertSection() now writes compiledText here, not Postgres.

---

## RAILWAY PROJECT

Project: `miraculous-nature` (68707c61-5c68-4f37-88fc-c301fd6b90e7)
Region: EU West, Pro plan
Scheduler: `Ingest-scheduler` (f3397bee-e588-4b95-921f-2e0f2f169cc5), redeployed
RAILWAY_API_TOKEN: in scrutinise-web/.env
