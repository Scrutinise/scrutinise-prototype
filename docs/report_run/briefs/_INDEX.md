# CC brief batch — Restoration Programme report run

Written 29 Aug 2026 by the Cowork session, in one batch, so the relay through Charlie runs about
once a day rather than eight times. Hand them to CC in this order.

| # | Brief | When | Blocked by | Roughly |
|---|---|---|---|---|
| B2 | `CCW-B2_argument_graph.md` | Sat 29 | — | 1–2 h |
| B3 | `CCW-B3_caselaw_absorption.md` | Sat 29, after B2 | — | 1–2 h |
| B4 | `CCW-B4_markup_verification.md` | Sun 30 | — | 30 min |
| B5 | `CCW-B5_register_resolution.md` | Sun 30 eve / Mon 31 | my `register_proposals.json` | 1–2 h |
| B6 | `CCW-B6_appendix_a_publication.md` | Wed 2 | the three inbound files, B5 | 30 min |
| B7 | `CCW-B7_starkey_transcript_corpus.md` | Any time, start early | — | 30 min hands-on, ~20 min waiting |

**B7 is independent of B2–B6** — different inputs, different outputs, no ordering constraint. Its first
phase is mostly waiting on the network, so it is worth kicking off before the others rather than after.

**B2, B3 and B4 can be relayed now — none of them waits on me.** B5 waits on the channel sweep.

## The relay line — one paste, not five

> Work through `docs/report_run/briefs/` in the order set out in `_INDEX.md`. Execute B2, B3 and B4
> now. **Stop before B5** — it is blocked on an input file the Cowork session has not produced yet;
> if `docs/report_run/register_proposals.json` does not exist, do not start B5 and do not invent its
> input. Report at the end of each brief. Standing rules in `CLAUDE.md` apply — in particular §12:
> no git during the run, one commit script at the end.

⚠ **B5's input file is the one thing that can go wrong here.** A brief whose input is missing is a
brief that gets improvised, and an improvised register is worse than no register. If it is not on
disk, stop and say so.

## What is not in this batch, and why

- **CRA 2005 at full depth (T4).** Decided against: three worked measures, not four. Its scoping row
  already exists in `scoping_remaining.csv` and it appears in the register like any other measure.
- **Re-extraction of the 2,894 duplicated `gid` documents.** A real defect, declared in CC's own
  `_README.md`, and not a report-run job. It goes on the list for after Thursday.
- **Anything interpretive.** Dispositions, whether a gate bites, whether the absorption claim holds —
  all mine. CC produces evidence; it draws no conclusions.
