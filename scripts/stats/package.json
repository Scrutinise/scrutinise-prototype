{
  "name": "scrutinise-stats",
  "version": "0.1.0",
  "private": true,
  "description": "Statistics layer (Phase A, UK spine) — separate DB from the corpus/app. See docs/STATS_LAYER_SPEC.md.",
  "scripts": {
    "generate": "prisma generate --config=prisma.config.ts",
    "migrate:diff": "prisma migrate diff --config=prisma.config.ts --from-config-datasource --to-schema prisma/schema.prisma --script",
    "migrate:deploy": "prisma migrate deploy --config=prisma.config.ts",
    "whichdb": "tsx --tsconfig ../tsconfig.json whichdb.ts",
    "seed": "tsx --tsconfig ../tsconfig.json seed-catalogue.ts",
    "refresh": "tsx --tsconfig ../tsconfig.json refresh-scheduler.ts",
    "verify": "tsx --tsconfig ../tsconfig.json verify.ts",
    "measure": "tsx --tsconfig ../tsconfig.json measure-pilot.ts",
    "compare": "tsx --tsconfig ../tsconfig.json compare-example.ts",
    "check:series-key": "tsx --tsconfig ../tsconfig.json check-series-key.ts",
    "check:coverage": "tsx --tsconfig ../tsconfig.json check-series-coverage.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.5.0",
    "@prisma/client": "^7.5.0",
    "dotenv": "^16.4.5",
    "pg": "^8.20.0",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "prisma": "^7.5.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
